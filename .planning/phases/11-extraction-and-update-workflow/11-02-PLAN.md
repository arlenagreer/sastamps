---
phase: 11-extraction-and-update-workflow
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .claude/skills/philatex-update/SKILL.md
autonomous: true
requirements: [CONT-01, WORK-02, WORK-03]

must_haves:
  truths:
    - "SKILL.md Step 5 spawns the philatex-newsletter-agent with correct context"
    - "SKILL.md contains post-agent steps that verify agent completion before checkpoint handoff"
    - "The TODO stub from Phase 10 is resolved and replaced with actual agent invocation"
  artifacts:
    - path: ".claude/skills/philatex-update/SKILL.md"
      provides: "Complete skill with agent spawning and post-agent verification"
      min_lines: 180
      contains: "philatex-newsletter-agent"
  key_links:
    - from: ".claude/skills/philatex-update/SKILL.md"
      to: ".claude/agents/philatex-newsletter-agent.md"
      via: "Task() spawn in Step 5"
      pattern: "philatex-newsletter-agent"
---

<objective>
Update the existing SKILL.md to wire in the philatex-newsletter-agent created in Plan 01. Replace the Phase 10 TODO stub with actual agent spawning instructions and add post-agent verification steps.

Purpose: Completes the skill-to-agent pipeline so the full extraction and update workflow is invocable via `/philatex-update <pdf-path>`.

Output: Updated SKILL.md with agent spawning, post-agent verification, and preparation for Phase 12 checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-extraction-and-update-workflow/11-01-SUMMARY.md
@.claude/skills/philatex-update/SKILL.md
@.claude/agents/philatex-newsletter-agent.md

<interfaces>
<!-- From SKILL.md (Phase 10 output) - current Step 5 structure -->
Step 5 currently says:
  1. Report: "Edition {ID} is new. Proceeding with extraction and update workflow."
  2. Pass context to Phase 11 agent (PDF path, year, quarter, edition ID)
  3. TODO: Phase 11 will define the agent at .claude/agents/philatex-newsletter-agent.md

Post-agent behavior note: "The skill resumes after the agent completes to handle the human checkpoint (Phase 12). The commit happens in the skill after checkpoint approval, not in the agent."

<!-- From agent definition (Plan 01 output) - Step 9 summary format -->
Agent returns a structured summary containing:
  - Files created (list with paths)
  - Files modified (list with paths)
  - Count of meetings added
  - Count of ICS files generated
  - Count of [UNVERIFIED] markers (with itemized list)
  - Schema validation warnings
  - Out-of-scope change recommendations
  - Proofreading report location
  - Build status (success/failure)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md with agent spawning and post-agent steps</name>
  <files>.claude/skills/philatex-update/SKILL.md</files>
  <action>
Read the current SKILL.md and make these targeted updates:

**1. Replace Step 5's TODO stub with agent spawning instructions:**

Remove the line `> TODO: Phase 11 will define the agent at .claude/agents/philatex-newsletter-agent.md` and replace with concrete Task() spawning instructions:

```markdown
## Step 5: Extraction and Update Workflow

When the duplicate check passes:

1. Report: "Edition {ID} is new. Proceeding with extraction and update workflow."
2. Spawn the extraction agent with the following context:

   Agent: `philatex-newsletter-agent`
   Context to pass:
   - PDF_PATH: the validated PDF file path (absolute)
   - EDITION_YEAR: the identified year (e.g., 2026)
   - QUARTER_NAME: the identified quarter name (e.g., "Third")
   - EDITION_ID: the constructed edition ID (e.g., "2026-Q3")

   Files the agent must read before starting:
   - `.claude/agents/philatex-newsletter-agent.md` (its own instructions)
   - `data/schemas/newsletter.schema.json` (validation target)
   - `data/schemas/meeting.schema.json` (validation target)
   - `data/newsletters/newsletters.json` (current catalogue)
   - `data/meetings/meetings.json` (current meetings)

3. The agent handles all extraction, validation, data file updates, ICS generation, HTML updates, and ESBuild rebuild.
4. The agent does NOT commit. It returns a structured summary of all changes.
```

**2. Add Step 6: Post-Agent Verification**

After Step 5, add:

```markdown
## Step 6: Post-Agent Verification

After the agent completes, verify its work before proceeding to checkpoint:

1. Confirm the agent reported build success (`npm run build:js` exit code 0)
2. Confirm all expected files were modified:
   - `data/newsletters/newsletters.json` (must contain new edition ID)
   - `data/meetings/meetings.json` (must contain new quarter dates)
   - At least one ICS file in `data/calendar/` for the new quarter
   - Quarterly aggregate ICS in `public/`
   - `index.html`, `newsletter.html`, `meetings.html` updated
3. Count [UNVERIFIED] markers across all modified files
4. If the agent reported any errors or failed the build, report the failure and STOP

If verification passes, proceed to the human checkpoint (Phase 12).
```

**3. Update the "Post-agent behavior" comment:**

Replace the existing comment at the bottom of Step 5 with:

```markdown
**Post-agent behavior:** After Step 6 verification passes, the skill presents the human checkpoint (Phase 12). The checkpoint leads with key facts and [UNVERIFIED] items before showing diffs. The commit happens ONLY after human approval.
```

**4. Update Important Rules:**

Add rule 6: "After the agent completes, verify its summary before presenting the checkpoint. If the build failed or required files were not updated, do NOT proceed to checkpoint."

**5. Update the skill description in frontmatter** to remove the phrase "then hands off to the extraction and update workflow" and replace with "extracts content, updates site files, and presents changes for review" since the full workflow is now integrated.
  </action>
  <verify>
    <automated>grep -c "philatex-newsletter-agent" .claude/skills/philatex-update/SKILL.md | awk '{if ($1 >= 1) print "PASS: Agent reference found " $1 " times"; else {print "FAIL: No agent reference"; exit 1}}' && grep -c "TODO" .claude/skills/philatex-update/SKILL.md | awk '{if ($1 == 0) print "PASS: No TODO stubs remaining"; else {print "FAIL: " $1 " TODO stubs still present"; exit 1}}' && grep -c "Post-Agent Verification" .claude/skills/philatex-update/SKILL.md | awk '{if ($1 >= 1) print "PASS: Post-agent verification step present"; else {print "FAIL: Missing post-agent verification"; exit 1}}'</automated>
  </verify>
  <done>
SKILL.md updated with:
- Step 5 spawns philatex-newsletter-agent with correct context (PDF path, year, quarter, edition ID)
- Step 6 post-agent verification checks build success, file updates, and [UNVERIFIED] count
- TODO stub removed and replaced with concrete agent spawning instructions
- Important Rules updated with post-agent verification rule
- Frontmatter description updated to reflect complete workflow
  </done>
</task>

</tasks>

<verification>
1. SKILL.md references `philatex-newsletter-agent` by name
2. No TODO stubs remain in SKILL.md
3. Step 6 (Post-Agent Verification) exists with build check, file check, and [UNVERIFIED] count
4. The skill-to-agent-to-checkpoint pipeline is documented end-to-end
5. Important Rules section has 6 rules (original 5 + new verification rule)
</verification>

<success_criteria>
- `/philatex-update` skill has a complete pipeline from input through agent spawning through post-agent verification
- The TODO from Phase 10 is fully resolved
- The skill correctly defers to Phase 12 for the human checkpoint
- SKILL.md line count is at least 180 (expanded from 165)
</success_criteria>

<output>
After completion, create `.planning/phases/11-extraction-and-update-workflow/11-02-SUMMARY.md`
</output>
