---
phase: 12-human-checkpoint-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/philatex-update/SKILL.md
autonomous: true
requirements:
  - WORK-01

must_haves:
  truths:
    - "Agent pauses and presents a structured checkpoint before any git commit"
    - "Checkpoint leads with a 10-item key-facts summary (red flags first, content facts second, scope last)"
    - "Human can approve to proceed to commit, or reject to discard all staged changes"
    - "Conversational fix-and-retry works naturally — human types feedback instead of approve/reject"
    - "Rejection reverts all modified files and removes newly created files"
    - "Approval triggers a single atomic commit with conventional commit message"
  artifacts:
    - path: ".claude/skills/philatex-update/SKILL.md"
      provides: "Steps 7-9: Human Checkpoint, Approval/Rejection Handling, Commit"
      contains: "Step 7"
  key_links:
    - from: ".claude/skills/philatex-update/SKILL.md (Step 6)"
      to: ".claude/skills/philatex-update/SKILL.md (Step 7)"
      via: "Post-agent verification passes, then checkpoint is presented"
      pattern: "Step 7"
    - from: ".claude/skills/philatex-update/SKILL.md (Step 7)"
      to: "AskUserQuestion"
      via: "Checkpoint summary followed by approve/reject prompt"
      pattern: "Approve|Reject"
    - from: ".claude/skills/philatex-update/SKILL.md (Step 8)"
      to: "git checkout"
      via: "Rejection triggers revert of all permitted files"
      pattern: "git checkout"
---

<objective>
Add the human checkpoint workflow (Steps 7-9) to the /philatex-update SKILL.md, completing the v1.3 agent's review-before-commit capability.

Purpose: The checkpoint is the final safety gate in the philatex update workflow. It prevents wrong data (hallucinated dates, incorrect member names, stale fields) from being committed by surfacing a skimmable key-facts summary and requiring explicit human approval before any git commit runs. This directly satisfies WORK-01 and closes the Pitfall 5 (checkpoint fatigue) and Pitfall 3 (partial update) concerns from research.

Output: Updated SKILL.md with three new steps that complete the end-to-end workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-human-checkpoint-design/12-CONTEXT.md
@.claude/skills/philatex-update/SKILL.md
@.claude/agents/philatex-newsletter-agent.md

<interfaces>
<!-- The executor needs to understand the existing SKILL.md structure and the agent's Step 9 output format -->

From .claude/skills/philatex-update/SKILL.md:
- Steps 1-6 already exist (Input Parsing, File Validation, Edition Identification, Duplicate Check, Extraction and Update Workflow, Post-Agent Verification)
- Step 6 ends with: "If verification passes, proceed to the human checkpoint (Phase 12)."
- The `allowed-tools` frontmatter includes: Read, Write, Edit, Bash, Grep, Glob
- AskUserQuestion is NOT in allowed-tools but IS mentioned in 12-CONTEXT.md as available

From .claude/agents/philatex-newsletter-agent.md Step 9 (Summary Output):
- Agent returns a structured summary with: Edition info, Files Created, Files Modified, Data Statistics, [UNVERIFIED] Markers (with count and itemized list), Schema Validation results, Out-of-Scope Observations, Build Status
- The checkpoint consumes this summary to build the key-facts display

From 12-CONTEXT.md:
- D-01 through D-03: 10 fixed key-facts categories, red-flags-first ordering
- D-04 through D-06: Binary approve/reject with clean revert; conversational fix-and-retry
- D-07 through D-09: Category-grouped stat summaries for diffs
- D-10, D-11: All-or-nothing approval with fix-and-retry loop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 7 (Human Checkpoint Presentation) to SKILL.md</name>
  <files>.claude/skills/philatex-update/SKILL.md</files>
  <action>
Add `AskUserQuestion` to the `allowed-tools` list in the SKILL.md YAML frontmatter (it is confirmed available per 12-CONTEXT.md but not yet listed).

Then add Step 7 after the existing Step 6 section. Step 7 is the checkpoint presentation. It must contain the following, written as skill instructions the agent will follow:

**Step 7: Human Checkpoint**

The step begins by stating: "After Step 6 verification passes, present the human checkpoint. This is the ONLY place where the skill pauses for human input."

**7a. Key-Facts Summary (per D-01, D-02, D-03)**

Instruct the agent to build a 10-item key-facts summary from the agent's Step 9 output. The 10 items are FIXED categories in this EXACT order (red-flags-first per D-02):

1. `[UNVERIFIED] count` — "0 items" or "N items — REVIEW REQUIRED". Source: agent summary [UNVERIFIED] Markers count.
2. `Schema validation status` — "All passed" or "N warnings: {details}". Source: agent summary Schema Validation.
3. `Newsletter ID + title` — e.g., "2026-Q3: The Philatex — Third Quarter 2026". Source: agent summary Edition block.
4. `Quarter date range` — e.g., "July-September 2026". Derive from QUARTER_NAME context variable.
5. `Meetings added` — count and date span, e.g., "13 meetings, Jul 3 - Sep 25". Source: agent summary Data Statistics.
6. `Officer changes` — names and roles or "No changes". Source: agent summary (compare about.html modifications).
7. `New members` — count and names or "None". Source: agent summary announcements.
8. `Announcements` — count and brief list. Source: agent summary announcements.
9. `TSDA shows` — count and date range or "None". Source: agent summary.
10. `Files modified` — count with category breakdown, e.g., "17 files: 2 data, 5 HTML, 13 ICS, 1 build". Source: agent summary Files Created + Files Modified.

Instruct: "Present the 10 items as a numbered list. The entire summary MUST fit under 20 lines (per D-03). Items 1-2 are red flags — if either is non-zero/non-pass, add a visible warning line above the summary: '--- RED FLAGS DETECTED — Review items 1-2 carefully ---'."

**7b. Changes by Category (per D-07, D-08, D-09)**

After the key-facts summary, instruct the agent to show a "Changes by Category" section grouping modified files by type:

- **Data Files:** list each with `+N -M` line counts and semantic annotation (e.g., "newsletters.json (+45 -3) — 1 new edition entry")
- **HTML Pages:** list each with `+N -M` and annotation
- **ICS Calendar:** collapse to single summary line, e.g., "13 individual + 1 quarterly .ics files generated"
- **Build Output:** note rebuild status

Instruct: "Get line counts using `git diff --stat` on each modified file. This section shows SCOPE, not content — the key-facts summary already covers semantics. The human can run `git diff <file>` for line-by-line detail."

**7c. Proposed Commit Message**

Instruct the agent to propose a conventional commit message, e.g., `content(Q3-2026): add Third Quarter 2026 newsletter update`. Show it at the end of the checkpoint.

**7d. Approve/Reject Prompt (per D-04)**

Instruct: "After presenting the summary and diff overview, prompt the human for a decision. Present two explicit options: **Approve** (commit all staged changes) or **Reject** (revert all changes). The human may also type free-form feedback instead of selecting an option — this triggers the fix-and-retry loop (per D-06, D-11)."

Note in the instructions: "If the human types feedback (anything other than 'approve' or 'reject'), treat it as a correction request. Make the requested changes, then re-present the full checkpoint from 7a. This loop continues until the human approves or rejects."
  </action>
  <verify>
    <automated>grep -c "Step 7" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && grep -c "UNVERIFIED.*count" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && grep -c "AskUserQuestion" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md contains Step 7 with all 10 key-facts categories in red-flags-first order, a Changes by Category section, a proposed commit message, and an approve/reject prompt with fix-and-retry loop instructions. AskUserQuestion is in allowed-tools.</done>
</task>

<task type="auto">
  <name>Task 2: Add Steps 8-9 (Rejection Revert and Commit on Approval) to SKILL.md</name>
  <files>.claude/skills/philatex-update/SKILL.md</files>
  <action>
Add Step 8 and Step 9 after Step 7 in SKILL.md.

**Step 8: Rejection — Clean Revert (per D-04, D-05)**

Instruct the agent on what to do when the human selects "Reject":

1. Identify all files modified during this run. Source: the agent's Step 9 summary (Files Created + Files Modified lists).
2. For files that existed before the run (modified files): run `git checkout -- <file>` for each one to revert to HEAD state.
3. For files that were newly created during the run (new ICS files, new PDF in public/): run `rm <file>` for each one.
4. After revert, run `npm run build:js` to restore the build output to pre-run state.
5. Verify clean state: run `git status` and confirm no uncommitted changes remain in permitted-file scope.
6. Report: "All changes reverted. Working tree restored to pre-run state. No files were committed."

Instruct: "This is all-or-nothing (per D-10). Never partially revert. The entire update is treated as an atomic transaction — either everything is committed or nothing is."

**Step 9: Approval — Atomic Commit (per D-04)**

Instruct the agent on what to do when the human selects "Approve":

1. Stage all modified and newly created files: `git add <file>` for each file from the agent's Files Created + Files Modified lists. Do NOT use `git add .` or `git add -A` — only add files from the permitted-file scope.
2. Also stage the build output files in `dist/` that were regenerated by `npm run build:js`.
3. Commit with the proposed conventional commit message from Step 7c: `git commit -m "<message>"`.
4. Report: "Changes committed: <commit hash>. Run `git push` when ready to deploy."

Instruct: "Do NOT push. The human controls when to push to the remote."

**Update Important Rules section:**

Add these rules to the existing "Important Rules" section at the bottom of SKILL.md:

7. **The human checkpoint (Step 7) is MANDATORY.** Never skip it, even if all validations pass and there are zero [UNVERIFIED] markers.
8. **On rejection, revert ALL changes.** Never leave partial modifications. The working tree must return to the exact state before the skill was invoked.
9. **On approval, commit ALL changes atomically.** Use `git add` with explicit file paths, never `git add .` or `git add -A`.
10. **The fix-and-retry loop has no limit.** The human can request as many corrections as needed before approving or rejecting.
  </action>
  <verify>
    <automated>grep -c "Step 8" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && grep -c "Step 9" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && grep -c "git checkout" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && grep -c "git commit" .claude/skills/philatex-update/SKILL.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md contains Step 8 (rejection with clean revert via git checkout + rm for new files + rebuild) and Step 9 (approval with atomic commit using explicit git add per file). Important Rules section updated with rules 7-10 covering mandatory checkpoint, full revert, atomic commit, and unlimited fix-and-retry.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full SKILL.md structure:

1. **Step continuity:** Steps 1-9 exist in order, with no gaps or duplicates
   ```bash
   grep "^## Step [0-9]" .claude/skills/philatex-update/SKILL.md
   ```
   Expected: Steps 1 through 9 in sequence.

2. **Key-facts completeness:** All 10 categories present in Step 7
   ```bash
   grep -c "UNVERIFIED.*count\|Schema validation\|Newsletter ID\|Quarter date\|Meetings added\|Officer changes\|New members\|Announcements\|TSDA shows\|Files modified" .claude/skills/philatex-update/SKILL.md
   ```
   Expected: 10 matches.

3. **Allowed tools:** AskUserQuestion is in the frontmatter
   ```bash
   head -20 .claude/skills/philatex-update/SKILL.md | grep "AskUserQuestion"
   ```

4. **No deferred features present:** No interactive diff viewer, checkpoint history, or automated spot-check tests
   ```bash
   grep -ci "interactive diff\|checkpoint history\|audit log\|automated spot-check" .claude/skills/philatex-update/SKILL.md
   ```
   Expected: 0.
</verification>

<success_criteria>
1. SKILL.md has Steps 7-9 that implement the complete checkpoint workflow
2. Step 7 presents a 10-item key-facts summary in red-flags-first order (D-01, D-02, D-03) under 20 lines
3. Step 7 includes Changes by Category with file-grouped stat summaries (D-07, D-08, D-09)
4. Step 7 ends with an approve/reject prompt with conversational fix-and-retry (D-04, D-06, D-11)
5. Step 8 performs all-or-nothing revert on rejection (D-05, D-10) — git checkout for modified, rm for new, rebuild
6. Step 9 performs atomic commit on approval with explicit file paths (D-04)
7. AskUserQuestion is in the allowed-tools frontmatter
8. Important Rules updated with checkpoint/revert/commit rules
9. WORK-01 is satisfied: agent pauses for human review before committing any changes
</success_criteria>

<output>
After completion, create `.planning/phases/12-human-checkpoint-design/12-01-SUMMARY.md`
</output>
