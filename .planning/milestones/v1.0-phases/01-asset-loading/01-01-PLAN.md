---
phase: 01-asset-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - about.html
  - archive.html
  - contact.html
  - glossary.html
  - meetings.html
  - membership.html
  - newsletter.html
  - resources.html
  - search.html
  - 404.html
autonomous: true

must_haves:
  truths:
    - "Google Fonts (Open Sans 400/600/700, Merriweather 400/700) load without 404 errors on all pages"
    - "Favicon displays in browser tabs across all pages"
  artifacts:
    - path: "index.html"
      provides: "Correct font links, favicon links"
    - path: "meetings.html"
      provides: "Fixed duplicate display=swap in font URL"
    - path: "newsletter.html"
      provides: "Added missing favicon link"
    - path: "glossary.html"
      provides: "Added missing favicon link"
    - path: "membership.html"
      provides: "Added missing favicon link"
  key_links:
    - from: "all HTML pages"
      to: "https://fonts.googleapis.com/css2"
      via: "link rel=stylesheet"
      pattern: "fonts\\.googleapis\\.com/css2.*Merriweather.*Open.Sans"
    - from: "all HTML pages"
      to: "favicon.ico"
      via: "link rel=icon"
      pattern: "rel=\"icon\".*favicon"
---

<objective>
Fix Google Fonts loading and favicon references across all HTML pages so fonts load correctly and favicon displays in browser tabs.

Purpose: Eliminate font 404 errors and missing favicon across all pages (ASSET-01, ASSET-02)
Output: All 11 HTML pages with consistent, working font and favicon references
</objective>

<execution_context>
@/Users/arlenagreer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arlenagreer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Google Fonts URLs across all pages</name>
  <files>meetings.html</files>
  <action>
Fix the duplicate `&display=swap` parameter in meetings.html line 42. The current URL is:
```
https://fonts.googleapis.com/css2?family=Merriweather:wght@400;700&amp;family=Open+Sans:wght@400;600;700&amp;display=swap&display=swap
```
Change to:
```
https://fonts.googleapis.com/css2?family=Merriweather:wght@400;700&amp;family=Open+Sans:wght@400;600;700&amp;display=swap
```

All other pages already have the correct font URL format. Verify by grepping all HTML files for the font URL pattern to confirm consistency.

Per user decision: Use Google Fonts CDN with `font-display: swap`, latin subset only. The existing URLs already use this approach -- only the meetings.html duplicate needs fixing.
  </action>
  <verify>
Run: `grep -n "display=swap&display=swap\|display=swap&amp;display=swap" *.html` -- should return NO results.
Run: `grep -c "fonts.googleapis.com/css2" *.html | grep -v node_modules | grep -v ":0$"` -- should show all 11 main HTML pages plus q4_update.html and offline.html have font links.
  </verify>
  <done>All HTML pages reference Google Fonts with correct URL format (no duplicate parameters). Fonts load Open Sans 400/600/700 and Merriweather 400/700 with display=swap.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize favicon references across all pages</name>
  <files>newsletter.html, glossary.html, membership.html, resources.html, 404.html, index.html, about.html, archive.html, contact.html, meetings.html, search.html</files>
  <action>
The project has `favicon.ico` in the site root (202KB). Per user decision: use single favicon.ico in root directory with explicit link tag.

**Pages missing favicon links entirely (add after the Font Awesome link or in the icons section):**
- newsletter.html -- add `<link rel="icon" href="favicon.ico" type="image/x-icon">`
- glossary.html -- add `<link rel="icon" href="favicon.ico" type="image/x-icon">`
- membership.html -- add `<link rel="icon" href="favicon.ico" type="image/x-icon">`

**Pages with incorrect/inconsistent favicon paths (update to use root favicon.ico):**
- resources.html line 26: currently `images/favicon.png` -- change to `favicon.ico` with type `image/x-icon`
- archive.html lines 118-120: currently uses `dist/images/favicon.webp` with png type and apple-touch-icon -- simplify to single `<link rel="icon" href="favicon.ico" type="image/x-icon">`
- meetings.html lines 2856-2858: same as archive -- simplify to single favicon.ico link
- contact.html lines 2217-2219: same pattern -- simplify to single favicon.ico link
- about.html lines 2277-2279: same pattern -- simplify to single favicon.ico link
- index.html lines 2515-2517: same pattern -- simplify to single favicon.ico link
- search.html lines 41-42: similar pattern with 2 links -- simplify to single favicon.ico link

**404.html** line 44: already uses `images/favicon.ico` -- change to `favicon.ico` (root) for consistency.

For pages that have multi-line favicon blocks (3 link tags for icon + apple-touch-icon), replace all 3 with a single: `<link rel="icon" href="favicon.ico" type="image/x-icon">`

Per user decision: No apple-touch-icon or multiple size variants -- fix the immediate 404 only.
  </action>
  <verify>
Run: `grep -n "favicon" *.html | grep -v node_modules` -- all 11 main pages should reference `favicon.ico`.
Run: `grep -c 'rel="icon"' *.html | grep -v node_modules | grep -v ":0$"` -- all 11 main HTML pages should have exactly 1 favicon link each.
Verify file exists: `ls -la favicon.ico` -- confirms root favicon.ico is present.
  </verify>
  <done>All 11 HTML pages have a single, consistent favicon link pointing to root favicon.ico. No pages reference dist/images/favicon.webp or images/favicon.png for favicon.</done>
</task>

</tasks>

<verification>
1. Open each page in browser -- favicon should display in tab
2. Check browser console -- no 404 errors for font CSS files
3. Fonts render correctly (Open Sans for body, Merriweather for headings)
4. `grep -rn "favicon" *.html | grep -v node_modules` shows consistent favicon.ico references
5. `grep "display=swap" *.html | grep -v node_modules` shows no duplicate display=swap parameters
</verification>

<success_criteria>
- All 11 HTML pages load Google Fonts without 404 errors
- meetings.html no longer has duplicate display=swap parameter
- All 11 HTML pages have favicon link pointing to root favicon.ico
- Favicon displays in browser tabs across all pages
</success_criteria>

<output>
After completion, create `.planning/phases/01-asset-loading/01-01-SUMMARY.md`
</output>
