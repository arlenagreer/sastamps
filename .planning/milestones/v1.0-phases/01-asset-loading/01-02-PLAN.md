---
phase: 01-asset-loading
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 404.html
  - site.webmanifest
autonomous: true

must_haves:
  truths:
    - "No service worker 404 errors appear in browser console on any page"
    - "No manifest 404 errors appear in browser console on any page"
  artifacts:
    - path: "sw.js"
      provides: "Service worker file (already exists, no changes needed)"
    - path: "site.webmanifest"
      provides: "Web manifest with correct icon paths"
    - path: "404.html"
      provides: "Fixed manifest reference from manifest.json to site.webmanifest"
  key_links:
    - from: "404.html"
      to: "site.webmanifest"
      via: "link rel=manifest"
      pattern: "rel=\"manifest\".*site\\.webmanifest"
    - from: "site.webmanifest"
      to: "favicon.ico"
      via: "icons array src"
      pattern: "\"src\".*favicon"
---

<objective>
Fix service worker and manifest references to eliminate console 404 errors.

Purpose: Resolve manifest.json 404 on 404.html page and ensure manifest icon paths are valid (ASSET-03)
Output: All manifest references point to existing files, no console 404 errors
</objective>

<execution_context>
@/Users/arlenagreer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arlenagreer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix manifest reference in 404.html and validate manifest icon paths</name>
  <files>404.html, site.webmanifest</files>
  <action>
**Fix 404.html manifest reference:**
404.html line 46 references `manifest.json` which does not exist. All other pages reference `site.webmanifest` which does exist. Change 404.html line 46 from:
```html
<link rel="manifest" href="manifest.json">
```
to:
```html
<link rel="manifest" href="site.webmanifest">
```

**Fix site.webmanifest icon paths:**
The current site.webmanifest references `images/favicon.png` for its icons. The file `images/favicon.png` exists (202KB), so this path works. However, since the overall favicon strategy (Plan 01) standardizes on root `favicon.ico`, update the manifest icons to reference `favicon.ico` for consistency:

```json
"icons": [
    {
        "src": "favicon.ico",
        "sizes": "192x192",
        "type": "image/x-icon"
    },
    {
        "src": "favicon.ico",
        "sizes": "512x512",
        "type": "image/x-icon"
    }
]
```

Per user decision: Keep manifest minimal (name, short_name, icons, theme_color) -- just enough to stop 404s. The existing manifest already has this structure, just fix the icon paths.

**Service worker verification:**
sw.js exists at root. All service worker registrations reference `/sw.js`. No changes needed -- just verify:
- glossary.html inline script registers `/sw.js` -- correct
- js/pages/about.js registers `/sw.js` -- correct
- js/utils/service-worker.js registers `/sw.js` -- correct
- js/script.js registers `/sw.js` -- correct

Per user decision: Do not change any existing caching strategy -- only fix file references.
  </action>
  <verify>
Run: `grep -n "manifest.json" *.html` -- should return NO results (all should reference site.webmanifest).
Run: `grep -n 'rel="manifest"' *.html | grep -v node_modules` -- all manifest references should point to site.webmanifest.
Run: `ls -la sw.js site.webmanifest` -- both files must exist.
Run: `cat site.webmanifest | python3 -m json.tool` -- valid JSON with icon paths pointing to existing files.
  </verify>
  <done>404.html references site.webmanifest instead of non-existent manifest.json. site.webmanifest has valid icon references. sw.js exists and all registrations point to correct path. No service worker or manifest 404 errors in browser console.</done>
</task>

</tasks>

<verification>
1. Open 404.html in browser -- no manifest.json 404 in console
2. Open any page with manifest reference -- no 404 for site.webmanifest
3. Check browser console on all pages -- no sw.js or manifest 404 errors
4. `grep "manifest.json" *.html` returns empty (no references to non-existent file)
</verification>

<success_criteria>
- 404.html references site.webmanifest (not manifest.json)
- site.webmanifest contains valid icon paths to existing files
- sw.js exists and all service worker registrations point to /sw.js
- No service worker or manifest 404 errors in browser console on any page
</success_criteria>

<output>
After completion, create `.planning/phases/01-asset-loading/01-02-SUMMARY.md`
</output>
