---
phase: 02-build-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - glossary.html
autonomous: true

must_haves:
  truths:
    - "Glossary page footer has no broken sitemap.xml link"
    - "No link to sitemap.xml exists anywhere on the Glossary page"
  artifacts:
    - path: "glossary.html"
      provides: "Glossary page with sitemap link removed"
  key_links: []
---

<objective>
Remove the broken sitemap.xml link from the Glossary page footer.

Purpose: The Glossary footer contains a link to sitemap.xml which does not exist, producing a 404 error when clicked (BILD-02). Per user decision, the link should be removed entirely rather than creating a sitemap.xml file. Only remove from glossary.html (the reported broken instance).

Output: glossary.html footer no longer contains a sitemap.xml link.
</objective>

<execution_context>
@/Users/arlenagreer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arlenagreer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key file:
- glossary.html line 306: `<p><i class="fas fa-sitemap"></i> <a href="sitemap.xml">Site Map</a></p>`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove sitemap link from Glossary footer</name>
  <files>glossary.html</files>
  <action>
    In glossary.html, find and remove the entire line containing the sitemap link in the footer:
    ```html
    <p><i class="fas fa-sitemap"></i> <a href="sitemap.xml">Site Map</a></p>
    ```
    This is located around line 306 in the "Social & Resources" footer section. Remove the entire `<p>` element. Do not replace it with another link. Do not modify any other part of the footer or page.
  </action>
  <verify>
    Grep glossary.html for "sitemap" -- the only match should be the Font Awesome class "fa-sitemap" if any icon remains, but ideally no matches at all since the entire `<p>` containing both the icon and link is removed.
    Verify the HTML structure around the removal point is still valid (no orphaned tags).
  </verify>
  <done>
    The sitemap.xml link is completely removed from glossary.html. No broken link to sitemap.xml exists. The footer renders correctly without the removed link.
  </done>
</task>

</tasks>

<verification>
1. Grep glossary.html for "sitemap.xml" -- should return no matches
2. Start dev server and load glossary.html -- footer renders correctly, no broken sitemap link visible
</verification>

<success_criteria>
The Glossary page footer no longer contains any link to sitemap.xml. The footer section renders correctly with the remaining links.
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-deployment/02-02-SUMMARY.md`
</output>
